package org.tessera
// Android companion — spec 10.5 REQUIRED
// Kotlin + Compose Material3, shared C++ core via FFI, Keystore
class MainActivity // : ComponentActivity — stub, builds with Android SDK
